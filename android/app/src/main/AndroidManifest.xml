<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Ollama(로컬 네트워크) 호출용 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 기기 재시작 후에도 리스너가 다시 붙도록 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="AI 가계부"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!--
            usesCleartextTraffic:
            Ollama 는 사용자의 PC 에서 http:// 로 동작한다.
            외부 인터넷이 아니라 로컬 네트워크(예: 10.0.2.2, 192.168.x.x)로만
            요청하지만, Android 9+ 는 기본적으로 평문 HTTP 를 막으므로 허용이 필요하다.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            결제 알림 수집 서비스.

            android:exported="true" 가 필요하다. 시스템(NotificationManager)이
            이 서비스에 바인딩하기 때문이다. 대신
            BIND_NOTIFICATION_LISTENER_SERVICE 권한으로 보호되므로
            시스템 외의 앱은 바인딩할 수 없다.
        -->
        <service
            android:name=".PaymentNotificationListenerService"
            android:exported="true"
            android:label="AI 가계부 결제 알림 수집"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Flutter 텍스트 처리 인텐트 쿼리 (템플릿 기본값) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
